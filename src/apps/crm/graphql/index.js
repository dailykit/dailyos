import {
   CUSTOMERS_COUNT,
   TOTAL_REVENUE,
   CUSTOMER_ISTEST,
   COUPON_LISTING,
   CAMPAIGN_LISTING,
   COUPON_TOTAL,
   COUPON_DATA,
   CAMPAIGN_DATA,
   CAMPAIGN_TOTAL,
   CAMPAIGN_TYPE,
   REWARD_TYPE,
} from './subscriptions'
import {
   CUSTOMERS_LISTING,
   CUSTOMER_DATA,
   ORDERS_LISTING,
   ORDER,
   ALL_DATA,
   STATUS,
   SUBSCRIPTION,
   SUBSCRIPTION_PLAN,
   OCCURENCES,
} from './queries'

import {
   ISTEST,
   COUPON_ACTIVE,
   CAMPAIGN_ACTIVE,
   CREATE_COUPON,
   CREATE_CAMPAIGN,
   UPDATE_COUPON,
   UPDATE_CAMPAIGN,
   DELETE_COUPON,
   DELETE_CAMPAIGN,
   CREATE_REWARD,
} from './mutations'

export {
   CUSTOMERS_COUNT,
   TOTAL_REVENUE,
   CUSTOMERS_LISTING,
   CUSTOMER_DATA,
   ORDERS_LISTING,
   ORDER,
   ALL_DATA,
   STATUS,
   SUBSCRIPTION,
   SUBSCRIPTION_PLAN,
   OCCURENCES,
   ISTEST,
   CUSTOMER_ISTEST,
   COUPON_LISTING,
   COUPON_ACTIVE,
   COUPON_TOTAL,
   CREATE_COUPON,
   COUPON_DATA,
   UPDATE_COUPON,
   DELETE_COUPON,
   CAMPAIGN_TOTAL,
   CAMPAIGN_ACTIVE,
   CAMPAIGN_DATA,
   DELETE_CAMPAIGN,
   CREATE_CAMPAIGN,
   UPDATE_CAMPAIGN,
   CAMPAIGN_LISTING,
   CAMPAIGN_TYPE,
   REWARD_TYPE,
   CREATE_REWARD,
}
