import { RECIPES, PRODUCTS, COLLECTIONS } from './queries'

import {
   CREATE_PRODUCT,
   UPDATE_PRODUCT,
   UPDATE_COLLECTION,
   CREATE_COLLECTION
} from './mutations/'

export {
   RECIPES,
   CREATE_PRODUCT,
   UPDATE_PRODUCT,
   PRODUCTS,
   CREATE_COLLECTION,
   UPDATE_COLLECTION,
   COLLECTIONS
}
