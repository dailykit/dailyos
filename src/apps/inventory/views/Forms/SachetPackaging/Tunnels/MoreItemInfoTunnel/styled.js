import styled from 'styled-components'

import { FlexContainer } from '../../../styled'

export const PaddedInputGroup = styled(FlexContainer)`
   justify-content: space-between;
   padding: 15px;
   border: 1px solid #ededed;
`
